stages:
  - build
  - deploy

build:
  stage: build
  only:
    - main
  before_script:
    - chmod +x ./gradlew
  script:
    - ./gradlew clean build -x test
    - ls -al build/libs
    - ARCHIVE="build/libs/$(ls build/libs | grep SNAPSHOT.jar)"
    - TARGET="tax-accounting:$(date "+%Y.%m.%d")"
    - docker build --build-arg ARCHIVE_FILE=$ARCHIVE -t $TARGET .
