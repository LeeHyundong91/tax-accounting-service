variables:
  REPOSITORY_HOST: 526459572421.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com

stages:
  - build
  - package
  - deploy

gradle:
  stage: build
  image: amazoncorretto:17
  only:
    - main
  before_script:
    - chmod +x ./gradlew
  script:
    - ./gradlew clean build -x test
    - mkdir target
    - cp build/libs/$(ls build/libs | grep SNAPSHOT.jar) ${CI_PROJECT_DIR}/service.jar
  artifacts:
    paths:
      - service.jar

docker:
  stage: package
  before_script:
    - aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username AWS --password-stdin ${REPOSITORY_HOST}
  script:
    - ls -al .
    - TARGET="tax-accounting:$(TZ="Asia/Seoul" date "+%y.%m.%d")"
    - docker build --build-arg ARCHIVE_FILE=service.jar -t ${TARGET} .
    - docker tag ${REPOSITORY_HOST}/dv-service.${TARGET} ${TARGET}
    - docker push ${REPOSITORY_HOST}/dv-service.${TARGET}
